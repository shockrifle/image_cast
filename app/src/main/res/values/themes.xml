<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        The Cast framework's MediaRouteButton and its dialogs require an
        AppCompat theme, so the app theme derives from Theme.AppCompat.
        Compose draws the rest of the UI with Material 3.
    -->
    <style name="Theme.ImageCast" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
    </style>
</resources>
